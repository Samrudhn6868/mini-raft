# Logs

- `final-pass.log` contains the full passing test run from `fixed-final-suite.log`.
- `failover-demo.log` is a short cleaned snippet that shows leader election, leader failure, and recovery.
- `debug/` contains older development and failing logs kept only for reference.
